# In dev, leave unset — Vite's proxy in vite.config.js forwards /api to localhost:8000.
# In production, point this at your deployed FastAPI backend, e.g.:
# VITE_API_BASE=https://researchmind-backend.onrender.com
VITE_API_BASE=
